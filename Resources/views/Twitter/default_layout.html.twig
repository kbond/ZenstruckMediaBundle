<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />
    <title>{% block title %}{% endblock %}</title>

    {% include 'ZenstruckMediaBundle:Twitter:_css.html.twig' %}
</head>
<body>
    <div class="container">
        {% for type, flashMessages in app.session.flashBag.all %}
            {% for flashMessage in flashMessages %}
                <div class="alert alert-{{ type }}">
                    <a class="close" data-dismiss="alert" href="#">&times;</a>
                    {{ flashMessage }}
                </div>
            {% endfor %}
        {% endfor %}

        {% block body %}{% endblock %}
    </div>

    {% include 'ZenstruckMediaBundle:Twitter:_javascripts.html.twig' %}
</body>
</html>
